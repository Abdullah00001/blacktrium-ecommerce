FROM node:20-alpine
WORKDIR /app
RUN apk add --no-cache dumb-init

COPY corn/package*.json ./
COPY corn/tsconfig.json ./
RUN npm install

# Copy corn sources after npm install
COPY corn/ ./

ENTRYPOINT ["/usr/bin/dumb-init", "--"]
CMD ["npm", "run", "dev"]
